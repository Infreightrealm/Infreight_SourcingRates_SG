"""
Models package.

Note: Individual modules can be imported directly (e.g., models.schemas)
without triggering database initialization. The full set of models
is only loaded when database operations are needed.
"""
