# Base URL of the Infreight FastAPI backend.
# Note: on a physical device, "localhost" points at the device itself — use your
# machine's LAN IP (e.g. http://192.168.1.10:8000) or the deployed Railway URL.
EXPO_PUBLIC_API_URL=http://localhost:8000
